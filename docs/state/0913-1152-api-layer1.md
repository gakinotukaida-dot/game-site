# 指示書：API・MCP 層1（`data/README_api.md` 1本追加）── game-site

- **版**＝v1.1（本人確定済み `0913-1152`＝数値の書き方は**A3**（隔離）／📍の較正値は**B1**（今日の実測へ更新）／形の不揃いは**(a)** README側で吸収し生成器は触らない。着手時に本文書を `docs/state/` へ commit し、土台にそのパスを書く＝`0810-1129-11` の型を踏襲）
- **設計の正**＝game-site 土台 `0810-1129`（本人確定 `-07`／実在リスク `-04`／実スキーマ `-08`／充足率 `-09`／骨子 `-11`）
- **ゴール（一文）**＝`data/README_api.md` を1本追加し、**Cowork の定時タスクがそのファイルだけを読んで `upcoming.json` の任意の1行を正しく解釈できる**状態にする。**コードは1行も触らず**、戻し方はファイル削除。

## 0. 着手前に読むもの
- 土台 `0810-1129` 全節（特に `-04` の実在リスク5件と `-11` の骨子8項目）／`0802E-05`（照会失敗と陰性を区別）／`0913-0221-G2`（消費者地図）／`0912-0735-G1`（較正の開示・本人確定）。
- 本指示書は **2026-09-13 に main の実物を clone して書いた**（`3f28aed`）。着手時に main が進んでいたら**実物を正とし、その旨を報告してから進める**。

## 1. 実測で決着した前提（★骨子 `0810-1129-08`/`-09` は1か月で古くなっていた）

2026-09-13 に main を clone して実測（データ生成は 2026-09-12）。

| 項目 | `0810-1129`（8/9〜8/10） | 本指示書（9/13 実測） |
|---|---|---|
| `data/` の成果物 | JSON 4本を計測 | **JSON 8本＋`rising_feed.xml`＝9本** |
| `upcoming.json` schema | `upcoming_v4` | **`upcoming_v5`** |
| 行の入れ物 | `rows`／`items` の2種 | **`features`（`prelaunch_model.json`）が加わり3種** |
| `view02_rising.json` | `meta` の下にメタ | **トップに `baseline_mode`／`up_thresh`、行に `al` が増えた** |
| `unknown_reason` | 3値 | **4値＋`null`**（`web_skipped_budget`・`twitch_key_absent` が増） |
| `release_known` true | 200/200 | **0/200** |
| `header_image`／`has_japanese` | 24／24 | **184／184** |
| `web_reach`／`web_news`／`web_views` | 40／21／16 | **34／25／6** |
| `price` 非null／`review` total>0 | 1／6 | **0／2** |
| 上位帯（40%以上）の較正 | 予測67%・実測36%・n=25 | **予測75.0%・実測35.1%・n=74** |
| 上位10%の的中倍率 | ×5.31 | **×5.96**（`validation_oos.top_decile_lift`・評価 n=1,678） |

**含意**＝骨子③「`0810-1129-09` の充足率と測定日をそのまま書く」は**採れない**。着手時の再実測が必須であり、しかも**再実測した値も同じ速さで腐る**。これが論点A（→A3）の根拠。

## 2. 成果物と、骨子8項目の割り当て

成果物＝**`data/README_api.md` 1本のみ**。骨子 `0810-1129-11` の8項目は次のとおり配置した。

| 骨子 | README の節 |
|---|---|
| ①4本のURLと更新頻度 | §1（**9本**へ拡張。cron を JST へ換算し「予定時刻であって成功保証ではない」と明記） |
| ②各キーの意味 | §3（`upcoming.json` 全27キー）・§4（`view02_rising.json`） |
| ③充足率と測定日 | **§6へ隔離**（A3） |
| ④空欄は陰性ではない | §0-1・§3-3・§3-4・§5 |
| ⑤`unknown_reason` の意味 | §5（**3値→4値＋null** へ更新） |
| ⑥`spike_prob` の較正の限界と `experimental` | §6-2・§4 |
| ⑦view02 だけ形が違う | §2（**`prelaunch_model.json` の `features` も追加**） |
| ⑧再配布を想定していない旨 | §8 |

## 3. A3（数値の隔離）の実装定義
- **腐らない性質**（キーの意味・三値・null の解釈・`unknown_reason`・安定の約束）は本文に書く。
- **腐る数値**（件数・充足率・較正値・倍率）は **§6 の1ブロックだけ**に置き、各値に**測定日**と**最新の取得先URL＋キー**を併記する。
- 更新するときは **§6 だけを差し替える**。本文には数値を書かない。
- ★**§6 を誰がいつ更新するかの条文は無い。** 規律12のモラトリアム下のため条文化せず、**9/30 の棚卸しの材料**とする。

## 4. やらないこと（線）
- **コード・ワークフロー・生成器・`index.html` に触らない。** 形の不統一は README 側で吸収する（論点C(a)）。
- **Neon 直結のAPIは作らない**（`0810-1129-04`-1。`0802D` の修理の前提を崩す）。
- **認証を足さない**（`-04`-3。新しい秘密を生まない）。
- **ホスティングを増やさない**（層1.5 は着手条件が成立していない）。
- 機械読みの推奨先は **`raw.githubusercontent.com`**。Pages は公開サイトと帯域を共有するため（`-03`）、機械読みを Pages に寄せない。

## 5. 合格条件（`0810-1129-11` を踏襲）
**Claude 以外の窓口（Cowork の定時タスク）から、`README_api.md` だけを読んで `upcoming.json` の任意の1行を正しく解釈できること。** 具体的には次の4つを README だけで答えられること。
1. `release` をそのまま日付としてパースしてよいか（→ `release_known` を見る）
2. `price: null` を「無料」と読んでよいか（→ 読んではいけない）
3. `has_japanese: null` を「日本語非対応」と読んでよいか（→ 読んではいけない）
4. 「上位10%は×5.96」とだけ報告してよいか（→ 較正の併記が要る）

## 5-1. ★自己監査の結果（v1.0 は不合格だった・圧縮しない）

`data/upcoming.json` の実物の1行（appid 4163960）を取り出し、**README だけで全27キーを解釈できるか**を機械照合した。**v1.0 では欠落8件**。

1. ★**データ内の時刻が UTC だと書いていなかった。** README §1 の更新時刻表は JST。**混ぜれば9時間ずれる。**
2. ★**`review` の中身（`positive`/`total`/`pct`）が未文書。** 実物は `{"positive":0,"total":0,"pct":null}` で、**`pct: null` は「好評率0%」ではなく母数0で割合が出せない**の意味。**README が自分で掲げた「空欄は陰性ではない」を自分で破っていた。**
3. `detail` の10キーが未文書。特に `siblings` は**同じ開発元の別作品のCCU**で、この作品の数字ではない。`detail.price`/`review`/`release` はトップレベルとの重複で、どちらが正か未定義だった。
4. `factors.bucket` の値域が未文書（実測＝`paid`/`hot`/`high`/`mid`/`low`。数値帯とカテゴリ値が混在）。`dir` は実測335件すべて `up`＝押し下げ要因は出力されない、を書いていなかった。
5. トップレベルの `model`（`base_rate`・`hit_threshold`・`readiness`・`validation_oos`）が未文書。**`expect` の「基準率」と `spike_prob` の「1,000人」を README の外で確認できる唯一の場所**だった。
6. `conf` の判定規則が「材料の量×検証強度」としか書いておらず判定できなかった。
7. `twitch_peak: null`（未取得）と `streamers: 0`（実測0）の差を説明していなかった。**§0-1 の区別の実例そのもの。**
8. `spike_prob` から §6-2（較正）への導線が無く、**先頭行の `0.9883`＝98.8% を額面で読める経路**が残っていた。

v1.1 で8件とも修理し、再監査で**全キー網羅（トップレベル8／行27／`detail`10／`review`3／`factors`3／view02 の4系統）**を機械確認した。

## 6. 外れの検出（`0805-2330-11` の型）
**2週間後（2026-09-27 目安）に「JSONが重くて結局使わなかった」場面が実際に出たか。** 出れば層1.5（薄いリモートMCP）へ、**出なければMCPは作らない。** 9/30 の棚卸しで判定する。

## 7. 戻し方
PR の Revert 1回（または `data/README_api.md` と本指示書の2ファイルを削除する1コミット）。記録側＝土台の該当節を削除し、📍の置換を逆向きに戻し、当月ログの節を削除する。**生成物・ワークフロー・DBには何も起きない。**

## 8. 未確認・申し送り（正直に残す）

1. ★**`release_known` が 200/200 true（8/10）→ 0/200（9/13）へ反転している。** 実装上は「DBの `release_date` 列が非NULLか」で、現在は200件すべてNULL＝`release` は Steam の表示文字列のまま出ている。**8/8 は「1件のみ真」、8/10 は「200/200 真」で、これで3回目の反転。** 本件は層1の担当外なので**修理していない**（`0810-1129-10` の型＝副産物の判定は担当セッションに委ねる）。**値の揺れ自体が仕様なのか欠陥なのかは未確認。**
2. **`data/cause_scorecard.json` は main に存在しない。** `.github/workflows/cause_scorecard.yml` は存在するが **schedule がコメントアウトされており `workflow_dispatch` のみ**＝一度も生成されていないと見られる。**（当初これを「定時実行されているのに成果物が無い＝沈黙した失敗」と読み違えた。コメント行を grep が拾ったためで、誤りだった。）** 土台 `0913-0221-G2` の「成果物10本」との1本差がこれに当たるかは**未確認**。README には**存在しないファイルを書いていない**。
3. **GitHub Pages の正確な公開オリジンは未確認**（`CNAME` なし＝`github.io` 直下と推定）。README では raw を正とし、Pages の絶対URLを書いていない。
4. **Pages が CORS ヘッダを返すか未確認**（`0810-1129-06` から未解消）。ブラウザから直接読む用途は README で触れていない。
5. **層2（他人配布）の法務下調べは未了**（`0810-1129-04`-5）。README §8 は「想定していない」と書くに留め、ライセンスを宣言していない。**ライセンスを書くことは層2の決定であり、層1で先取りしない。**
6. ★**README の公開は「リポジトリ外の消費者」を意図的に増やす。** `0913-0221-G2` は `rising_feed.xml` について「リポジトリ内の消費者ゼロなのに実際の読者は RSS と Discord＝機械検査では見えない」と記録している。📍の実在リスク3（出力形式の変更が下流を壊す・9/30判定）を**悪化させる方向**であり、9/30 の材料とする。
